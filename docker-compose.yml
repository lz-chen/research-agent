version: '3.8'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:80"
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
    ports:
      - "8501:8501"
    networks:
      - app-network

networks:
  app-network: